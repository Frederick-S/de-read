{
  "name": "de-read",
  "version": "0.0.4",
  "description": "Read number/year/time in German.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Frederick-S/de-read"
  },
  "author": "Frederick-S",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Frederick-S/de-read/issues"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "jshint": "jshint lib",
    "test": "mocha --reporter spec",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "devDependencies": {
    "mocha": "3.4.2",
    "coveralls": "2.13.1",
    "istanbul": "0.4.5",
    "mocha-lcov-reporter": "1.3.0",
    "jshint": "2.9.5"
  }
}
